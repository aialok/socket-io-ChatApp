<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Chat Lounge</title>
    <style>
        body {
            background-color: #f5f8fa;
            font-family: 'Arial', sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }

        form {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
            text-align: center;
        }

        input {
            width: 100%;
            padding: 10px;
            margin: 8px 0;
            box-sizing: border-box;
            border: 1px solid #ccd6dd;
            border-radius: 4px;
        }

        button {
            background-color: #1da1f2;
            color: white;
            padding: 12px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            width: 100%;
            font-size: 16px;
        }

        button:hover {
            background-color: #0c7ebd;
        }

        h1 {
            color: #1da1f2;
            font-size: 2em;
            margin-bottom: 20px;
        }

        p {
            color: #657786;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <form action="/chat" method="GET">
        <h1>Login to Chat Lounge</h1>
        <input type="text" name="username" placeholder="Username" required>
        <input type="text" name="roomId" placeholder="Room ID" required>
        <button type="submit">Log in</button>
       
    </form>
</body>
</html>
